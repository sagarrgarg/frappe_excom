# Lead sources — every way a lead enters Excom

One pipeline for all of them: `services/intake.py` → `Excom Intake Log` (raw payload, unique `dedupe_key`) → `map_payload` (field map per source, defaults below) → `crm_flow.resolve_or_create_lead` (attach to an existing Lead by email / phone, else create) → identity link → auto-ack template (optional) → SLA clock. Replay any log row from Admin → Intake sources / Desk.

Admin → Channels → **Intake sources** holds one row per source with mode, credentials, restrictions and the field map. Cron `*/5` runs every enabled Pull/Both source whose frequency elapsed (`tasks/intake.pull_due_sources`); `stale_source_alarm` logs when a source has had no success for 3× its frequency.

| Source | Mode | Entry point | Restrictions in place | Dedupe key | Default field map | Status |
|---|---|---|---|---|---|---|
| **IndiaMART** | **Pull** (authoritative, every 5 min, 5-min overlap) **+ Push** accelerator | Pull: `GET mapi.indiamart.com/wservce/crm/crmListing/v2/?glusr_crm_key=…&start_time&end_time` · Push: `POST /api/method/excom.excom.api.intake.indiamart_push?token=<push_token>` (set as the seller-panel push URL) | Push: per-source token (≥16 chars, Password field), optional **IP allowlist** (CIDR lines), 60 req/min per IP. Pull: CRM key stored as Password. | `indiamart:<UNIQUE_QUERY_ID>` | `SENDER_NAME→name, SENDER_MOBILE→phone, SENDER_EMAIL→email, SENDER_COMPANY→company_name, SENDER_CITY/STATE→city/state, SENDER_COUNTRY_ISO→country (ISO→Country name), QUERY_MESSAGE→message, QUERY_PRODUCT_NAME→product, SUBJECT, QUERY_TYPE` | Code + payload test done; needs the real CRM key on the source. IndiaMART's own limits: key valid per seller account, API returns max 15 min windows historically — the poller overlaps, so nothing is missed. |
| **TradeIndia** | **Pull** only (every 15 min, 15-min overlap) | `GET <api_url from "My Inquiry API">?userid&profile_id&key&from_date&to_date&limit=200` | user id / profile id / key stored on the source; URL never hardcoded | `tradeindia:<rfi_id \| inquiry_id \| id>` | `sender_name→name, sender_mobile→phone (+91 default), sender_email, sender_co→company_name, sender_city/state/country, subject, message, product_name→product` | Code + mapping test done; needs URL + key. TradeIndia has no push. |
| **Meta Lead Ads** (Facebook/Instagram forms) | **API pull** (`/<form_id>/leads` with a 10-min overlap, every 5–15 min via the source's frequency) **+ webhook accelerator** (`leadgen` ids → Graph fetch) | Webhook: the existing Meta webhook URL (`utils/webhook.py`, `hub.verify_token` + HMAC `X-Hub-Signature-256` with the app secret); fetch `GET graph.facebook.com/v19.0/<leadgen_id>?fields=…`; reconcile `GET /<form_id>/leads?filtering=[time_created > watermark]` | Page token stored as Password; source matched by `form_id` then `page_id`; unmatched forms are logged and ignored; HMAC required once any app secret exists | `meta:<leadgen_id>` | `field_data[]` → `full_name→name, phone_number→phone, email, company_name, city, country`; `campaign_name`/`ad_id` kept in raw for attribution | Code done; needs Page token + App Review permission `leads_retrieval`; until approved the webhook delivers ids and the fetch fails (logged, replayable). |
| **Website form** (any site, headless) | **Push** from the browser | `POST /api/method/excom.excom.api.intake.submit_enquiry` with `source_token, submission_id, name, email, phone, message, company, city, country, page_url, utm{}, extra{}` (+ `OPTIONS` preflight) | per-source token; **Origin allowlist** (exact `https://host` lines, CORS echoed only for allowed origins); 10 req/min per IP; honeypot (`website` / `_hp`); **min-fill seconds** (`started_at`); optional **Turnstile / hCaptcha** secret; idempotent by `submission_id`; minimal responses (no enumeration) | `web:<source>:<submission_id>` | `name, email, phone, message, company→company_name, city, country`; `utm` → attribution via `crm_compat` | Live; add one Website source per site (token + origin). |
| **Any partner / ERP / form builder (Zapier, Tally, Elementor, Google Forms via Apps Script, a distributor portal)** | **Token webhook** | `POST /api/method/excom.excom.api.intake.website_webhook?token=<push_token>` — JSON or form body; token also accepted in header `X-Excom-Token` or body `token` | per-source token; **IP allowlist** (CIDR) and Origin allowlist when a browser sends it; 60 req/min per IP; honeypot; idempotent by `submission_id` / `id` / `entry_id` / `response_id` or a hash of the payload | `web:<source>:<id>` or `web:<source>:sha1:<payload>` | defaults + the source's Field Map rows (`source_key → target_fieldname [transform]`) | Live. Admin → Intake sources → open a Website source → **Connect your website form** panel shows the token, both endpoints, an HTML form, a JS snippet and a curl example, with Generate / Regenerate token. |
| **Exhibition / trade fair** | Manual | Excom → New conversation → "New contact", or Desk Lead with Source = Exhibition and the `exhibition` field; bulk: Data Import of Lead with `intake_source` set | user permissions only | none (Lead per row) | — | Ready. |
| **Manual** (walk-in, phone call, referral) | Manual | same as above; `first_touch_channel` = Call / Manual | — | — | — | Ready. |
| **Organic WhatsApp** | Inbound message (Meta webhook, already live) | conversation appears in the inbox; ⋯ → *Promote to Lead* (or first reply auto-claims + you promote) | HMAC-verified webhook; per-account | thread + identity | `promote_thread` builds the Lead from the identity (name, phone, WhatsApp, account company), source "Organic WhatsApp" | Live. |
| **Organic Email** | Gmail poll (already live) | same: Promote to Lead from the thread | OAuth per account | thread | source "Organic Email" | Live. |
| **Web chat widget** | Visitor session (already live) | `<script src="<site>/assets/excom/widget/excom-chat.js" data-account="<account>" data-site="<site>" defer>` — shown with a Copy button on the web chat account in Admin | session token per visitor, IP-rate-limited guest endpoints, CORS from the widget | thread | source "Organic Web Chat" | Live. |
| **Instagram DMs** | **API poll** every minute (source of truth) + webhook accelerator | `GET graph.facebook.com/<ver>/<page_id>/conversations?platform=instagram&fields=…messages{…}` with a 10-min overlap; webhook `entry[].messaging[]` (object `instagram`) goes through the same ingest | Page token (Password) with `instagram_manage_messages`; HMAC-verified webhook; own messages skipped by page/IG id; idempotent by message id | `provider_message_id` = Graph message id | conversation → thread per IGSID; identity keyed by IGSID (no phone); ⋯ Promote to Lead as for WhatsApp | Built 2026-09-03 (`channels/meta_dm/service.py`); reply within 24h, HUMAN_AGENT tag up to 7 days when approved. Needs page id, IG account id, page token on an Instagram channel account. |
| **Facebook Messenger DMs** | same as Instagram (`platform=messenger`, object `page`) | same | `pages_messaging` | same | same | Built; needs page id + page token on a Messenger channel account. |
| **Justdial / Amazon Business / Alibaba / 99acres-style marketplaces** | not built | would be a new adapter under `excom/excom/intake/adapters/` following the IndiaMART shape (pull with overlap, `ingest(src, "<vendor>:<id>", row)`) | — | — | — | Each is ~50 lines once the vendor's API doc is in hand. |

## Meta in one place — Admin → Channels → Meta Business
One **Excom Meta Connection** per Business Manager: app id + app secret (signs webhooks), a **system-user token** (never expires; scopes `pages_show_list pages_messaging pages_read_engagement instagram_basic instagram_manage_messages leads_retrieval whatsapp_business_management business_management`), webhook verify token, company.
**Discover** lists what that token can see — Facebook pages (+ the Instagram account behind each), lead forms per page, WhatsApp numbers per WABA — and **Enable** on a row creates or updates the right record: Page → Messenger channel account · Instagram → Instagram channel account · Lead Form → Intake Source (Meta Lead Ads, pull every 15 min + webhook) · WhatsApp Number → WhatsApp channel account (system token, app secret, verify token). Enabling also subscribes the page to the app's webhook; polling is unaffected if that fails. Disable sets the record Inactive without deleting history. A short-lived user token can be exchanged for a 60-day one from the same screen; system-user tokens are recommended instead.

## Choosing the mode per source
- **Vendor offers an inquiry API** → Pull with an overlap window; it is authoritative and self-healing. Add their push URL only as an accelerator (IndiaMART).
- **Vendor only pushes** (webhooks) → require a per-source token in the URL or body, verify HMAC when the vendor signs, allowlist their IPs when they publish them, and schedule a reconcile if any list endpoint exists (Meta).
- **Your own pages / partners** → `submit_enquiry` with token + origin (browser) or token + IP (server), captcha for public forms.

## Security contract (applies to every guest entry point)
Per-source secret (Password field, never in the repo) · origin or IP allowlist · IP rate limit · idempotent dedupe key · raw payload kept 90 days (`purge_old_payloads`) · replay from the log · no enumeration (unknown token ⇒ generic 401) · nothing is sent back to the customer unless the source has an auto-ack template configured.

## To go live on each vendor
1. Admin → Intake sources → New: type, company, WhatsApp account (for auto-ack), mode, frequency, credentials, allowlist, SLA target, default owner or an Assignment Rule.
2. IndiaMART: paste the CRM key; optionally set the push URL in the seller panel with `?token=`.
3. TradeIndia: paste the "My Inquiry API" URL, user id, profile id, key.
4. Meta: subscribe the app to the `leadgen` field on the page; paste the page token; set form id (or page id for all forms).
5. Website: copy the token into the form's JS; add the site origin; optionally captcha secret.
6. Watch Admin → Overview and Intake → the SLA chip; `stale_source_alarm` tells you when a pull stops.
